import { defineConfig } from 'vite';

export default defineConfig({
  // Relative asset paths: the build then runs anywhere — GitHub Pages under
  // /IncrementalArcade/, an itch.io zip, or a local file server.
  base: './',
});
